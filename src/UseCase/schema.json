{
    "$schema": "http://json-schema.org/schema",
    "$id": "UseCase",
    "title": "UseCase Options",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the Use Case"
        },
        "feature": {
            "type": "string",
            "description": "The name of the feature this Use Case belongs to"
        },
        "flat": {
            "type": "boolean",
            "description": "Does not create a separate directory for the UseCase if true",
            "default": false
        }
    },
    "required": ["name", "feature"]
}
